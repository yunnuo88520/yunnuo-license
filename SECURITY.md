# 安全策略

## 支持范围

项目尚未发布稳定版本。目前仅维护默认分支的最新代码；发现问题后可能直接在默认分支修复，
不保证为早期提交提供补丁。

## 报告安全问题

请通过 GitHub 仓库的 **Security > Report a vulnerability** 私密报告安全问题，不要创建公开
Issue。仓库启用 Private vulnerability reporting 后，维护者会在该渠道确认并跟进。

报告中请尽量包含：

- 受影响版本或提交
- 可复现步骤与最小示例
- 影响范围和可能的利用方式
- 已知缓解措施（如有）

请勿在未经许可的生产系统或第三方数据上测试。维护者会尽量在 7 天内确认报告，并在完成修复
和发布安排后协调披露。

## 部署责任

生产部署必须覆盖所有开发默认凭据，使用 HTTPS，限制管理端网络访问，并定期备份数据库。
密钥和数据库凭据不应写入镜像、源码或公开日志。
