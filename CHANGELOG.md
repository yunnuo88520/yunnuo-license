# 更新记录

本项目采用 [语义化版本](https://semver.org/lang/zh-CN/)：主版本表示不兼容变更，次版本表示向后兼容的新功能，修订版本表示向后兼容的问题修复。

## [0.2.0] - 2026-08-19

### 新增

- 增加系统版本接口、构建信息、结构化更新记录和页面版本入口。
- Docker 构建支持注入版本、Git 提交和构建时间，并写入 OCI 镜像标签。
- 公共授权查询支持域名、IP、QQ、手机号和业务账号，匹配结果按隐私规则脱敏。
- 增加管理员站点品牌设置，网站名称、浏览器标题、Logo 和网站图标持久化到数据库。
- 增加首次启动初始化向导，可选择 SQLite 或外部 MySQL 并创建首个超级管理员。

### 变更

- 公共查询、管理后台和代理工作台迁移至 Vue 3、TypeScript 与 Vite。
- 重构桌面与移动端布局，界面图标统一使用 Lucide。
- 更新记录调整为仅管理员登录后可见，公开查询页不再暴露内部版本信息。
- CI 增加前端类型检查和生产构建，Docker 仅复制构建产物。
- 初始化配置使用原子持久化与并发保护，Docker 中的配置及 SQLite 数据默认写入持久卷。

## [0.1.0] - 2026-08-18

### 新增

- 提供产品、卡密、授权、代理、风控和审计核心能力。
- 支持在线验证、离线授权与 Go、Java、Node.js SDK。
- 支持内置 MySQL 的单 Docker 镜像部署。
